utils::globalVariables(c("cave_id", "logger_id", "name", "cave_name", "light_zone", "residuals", "var", "tm", "dp", "rh", "datetime", "zone", "val", "latitude", "longitude"))
# Package-wide environment for storing Shiny data
.ICCP_env <- new.env(parent = emptyenv())
